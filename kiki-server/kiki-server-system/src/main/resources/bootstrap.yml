spring:
  application:
    name: kiki-server-system
  cloud:
    nacos:
      discovery:
        server-addr: ${nacos.url}:8001
      config:
        server-addr: ${nacos.url}:8001
        group: DEFAULT_GROUP
        prefix: kiki-server-system
        file-extension: yaml