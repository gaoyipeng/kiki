spring:
  application:
    name: kiki-monitor-admin
  cloud:
    nacos:
      discovery:
        server-addr: ${nacos.url}:8001
      config:
        server-addr: ${nacos.url}:8001
        group: DEFAULT_GROUP
        prefix: kiki-monitor-admin
        file-extension: yaml