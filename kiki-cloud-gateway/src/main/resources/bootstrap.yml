spring:
  application:
    name: kiki-cloud-gateway
  cloud:
    nacos:
      discovery:
        server-addr: ${nacos.url}:8001
      config:
        server-addr: ${nacos.url}:8001
        group: DEFAULT_GROUP
        prefix: kiki-cloud-gateway
        file-extension: yaml